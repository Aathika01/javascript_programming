var showMessage;

showMessage = function (message) {
	console.log("The message is: " + message + " - Sent from JavaScript!");
};

showMessage("Hello, world!");
showMessage("JavaScript is awesome!");
showMessage("Functions are fun!");
