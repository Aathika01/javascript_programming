var msg;
var showMessage;

msg = "It's full of stars!";

showMessage = function () {
	console.log(msg);
};

showMessage();
// another msg
var msg;
var newMsg;
var showMessage;

msg = "It's full of stars!";
newMsg = "Explore the galaxy!";

showMessage = function () {
	console.log(msg);
};

showMessage(); 
// new and old msg
var msg;
var newMsg;
var showMessage;

msg = "It's full of stars!";
newMsg = "Explore the galaxy!";

showMessage = function () {
	console.log(msg);
	console.log(newMsg);
};

showMessage();
// joining two strings
var msg;
var newMsg;
var showMessage;

msg = "It's full of stars!";
newMsg = "Explore the galaxy!";

showMessage = function () {
	console.log(msg + " " + newMsg);
};

showMessage();
